# station-spring
Station - Spring Boot

``` POST bus```

``` DELETE bus/{id} ```

``` GET bus```

``` UPDATE bus/{id} ```

``` POST station/add ```

``` POST station ```

``` DELETE bus/{id} ```

``` PUT station/add ```

``` DELETE station/add ```

``` Register & Login```



