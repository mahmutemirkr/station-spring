# station-spring
Station - Spring Boot

``` POST bus```

``` DELETE bus/{id} ```

``` GET bus```

``` UPDATE bus/{id} ```

``` POST station/add ```

``` GET station ```

``` DELETE bus/{id} ```

``` PUT station/{id} ```

``` DELETE station/{id} ```

``` Register & Login```



