# station-spring
Station - Spring Boot

``` POST bus```

``` DELETE bus/{id} ```

``` GET bus/{id}```

``` GET bus/all```

``` GET bus/page { page & size & sort & direction } ```

``` PUT bus/{id} ```

``` POST station ```

``` GET station/{id} ```

``` PUT station/{id} ```

``` DELETE station/{id} ```

``` Register & Login```



